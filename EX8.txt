SQL> SELECT CNAME FROM DEPOSIT WHERE BNAME IN (    SELECT BNAME     FROM BRANCH     WHERE CITY = (        SELECT CITY         FROM BRANCH         WHERE BNAME = (SELECT BNAME FROM DEPOSIT WHERE CNAME = 'SUNIL')    ));

CNAME
---------------
ANIL
SUNIL

SQL> SELECT D.*, B.* FROM DEPOSIT D, BORROW B, CUSTOMERS C WHERE D.CNAME = C.CNAME AND B.CNAME = C.CNAME AND C.CITY = (SELECT CITY FROM CUSTOMERS WHERE CNAME = 'PRAMOD');

no rows selected
SQL> SELECT CNAME FROM CUSTOMERS WHERE CITY = 'NAGPUR' AND CNAME IN (SELECT CNAME FROM DEPOSIT) AND CNAME IN (SELECT CNAME FROM BORROW);

CNAME
---------------
MADHURI

SQL> SELECT EMPNO, EMPNAME
  2  FROM EMPLOYEE
  3  WHERE SAL > (SELECT AVG(SAL) FROM EMPLOYEE)
  4  ORDER BY SAL ASC;

     EMPNO EMPNAME
---------- ---------------
      7782 CLARK
      7698 BLAKE
      7566 JONES
      7902 FORD
      7788 SCOTT
      7839 KING

6 rows selected.
SQL> SELECT EMPNAME, SAL FROM EMPLOYEE WHERE MGR = (SELECT EMPNO FROM EMPLOYEE WHERE EMPNAME = 'FORD');

EMPNAME                SAL
--------------- ----------
SMITH                 8000

SQL> SELECT CNAME FROM DEPOSIT WHERE BNAME IN (SELECT BNAME FROM DEPOSIT WHERE CNAME='SUNIL');

CNAME
---------------
SUNIL

SQL> SELECT CNAME FROM DEPOSIT WHERE BNAME IN (    SELECT BNAME     FROM BRANCH     WHERE CITY = (        SELECT CITY         FROM BRANCH         WHERE BNAME = (SELECT BNAME FROM DEPOSIT WHERE CNAME = 'SUNIL')    ));

CNAME
---------------
ANIL
SUNIL

SQL> SELECT D.*, B.* FROM DEPOSIT D, BORROW B, CUSTOMERS C WHERE D.CNAME = C.CNAME AND B.CNAME = C.CNAME AND C.CITY = (SELECT CITY FROM CUSTOMERS WHERE CNAME = 'PRAMOD');

no rows selected
SQL> SELECT CNAME FROM CUSTOMERS WHERE CITY = 'NAGPUR' AND CNAME IN (SELECT CNAME FROM DEPOSIT) AND CNAME IN (SELECT CNAME FROM BORROW);

CNAME
---------------
MADHURI

SQL> SELECT EMPNO, EMPNAME
  2  FROM EMPLOYEE
  3  WHERE SAL > (SELECT AVG(SAL) FROM EMPLOYEE)
  4  ORDER BY SAL ASC;

     EMPNO EMPNAME
---------- ---------------
      7782 CLARK
      7698 BLAKE
      7566 JONES
      7902 FORD
      7788 SCOTT
      7839 KING

6 rows selected.
SQL> SELECT EMPNAME, SAL FROM EMPLOYEE WHERE MGR = (SELECT EMPNO FROM EMPLOYEE WHERE EMPNAME = 'FORD');

EMPNAME                SAL
--------------- ----------
SMITH                 8000

SQL> SELECT CNAME FROM DEPOSIT WHERE BNAME IN (SELECT BNAME FROM DEPOSIT WHERE CNAME='SUNIL');

CNAME
---------------
SUNIL